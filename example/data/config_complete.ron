/* @[crate::models::Config] */
Config(
    // Even though Config has Default, you can still specify all fields
    host: "localhost",
    port: 8080,
    max_connections: 100,
    debug: true,
    allowed_origins: [
        "http://localhost:3000",
        "https://example.com",
    ],
    api_key: Some(
        "secret-key-123",
    )
)
