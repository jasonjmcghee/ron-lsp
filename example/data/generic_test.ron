/* @[crate::models::GenericTest] */

GenericTest(
    // Good: Proper Option with Some
    good_option: Some("This is valid"),

    // Bad: Number instead of Option<String>
    bad_option: 12345,

    // Good: Proper Vec
    good_vec: [1, 2, 3, 4, 5],

    // Bad: Single integer instead of Vec
    bad_vec: 42,

    // Good: Proper HashMap
    good_hashmap: {
        "key1": 100,
        "key2": 200,
        "key3": 300,
    },

    // Bad: String instead of HashMap
    bad_hashmap: "not a map",

    // Good: Proper BTreeMap
    good_btreemap: {
        "enabled": true,
        "active": false,
    },

    // Bad: Integer instead of BTreeMap
    bad_btreemap: 123,

    // Good: Proper HashSet
    good_hashset: ["apple", "banana", "cherry"],

    // Bad: String instead of HashSet
    bad_hashset: "not a set",

    // Good: Proper Result with Ok
    good_result: Ok("Success!"),

    // Bad: Plain string instead of Result
    bad_result: "This should be Ok(...) or Err(...)",

    // Good: Proper Box
    good_box: 42,

    // Bad: String instead of Box<u32>
    bad_box: "not a number",
)
